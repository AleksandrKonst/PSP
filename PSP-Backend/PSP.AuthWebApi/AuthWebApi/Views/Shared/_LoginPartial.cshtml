@using Microsoft.AspNetCore.Identity
@using AuthWebApi.Models
@inject SignInManager<PspUser> SignInManager
@inject UserManager<PspUser> UserManager

<ul class="navbar-nav">
    <li class="nav-item">
        <form  class="form-inline" asp-controller="Auth" asp-action="Logout" asp-route-returnUrl="@Url.Action("Login", "Auth", new { area = "" })">
            <button type="submit" class="btn-outline-secondary btn btn-link logout">Выйти</button>
        </form>
    </li>
</ul>